plugins {
    id 'java-conventions'
    id 'checkstyle-conventions'
}

dependencies {
    implementation project(':common-lib')
    // No Spring Boot — pure Java game engine
}

jar {
    archiveBaseName = 'snake-game'
    archiveClassifier = 'engine'
}
