package com.airconsole.common.enums;

/**
 * Role of a player within a room.
 */
public enum PlayerRole {
    HOST,   // Created the room, can start games
    GUEST   // Joined the room, plays only
}
