spring:
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}

player-service:
  url: http://player-service:8082

signalr:
  hub-url: ${AZURE_SIGNALR_HUB_URL:http://notification-service:8084}
  access-token: ${AZURE_SIGNALR_ACCESS_TOKEN:}

logging:
  level:
    com.airconsole.notification: INFO
